## File: roles/base/meta/main.yml
## Dependencies: List roles required before executing this role, in order of execution

dependencies:
  - role: base_bootstrap
    when: base_run_bootstrap | default(false) | bool
    tags: [base, base_bootstrap]
  - role: base_packages
    when: not (base_run_bootstrap | default(false) | bool)
    tags: [base, base_packages]
  # - role: base_timezone
  # - role: base_ntp
  # - role: base_hostname
  # - role: base_sudo
  # - role: base_sshd
  # - role: base_firewall
  # - role: base_logging
  # - role: base_updates
  # - role: base_security
