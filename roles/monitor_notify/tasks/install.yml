---
# roles/monitor_notify/tasks/install.yml
# Install phase tasks for the `monitor_notify` role.

- name: "Install | Ensure monitor_notify packages are installed"
  ansible.builtin.apt:
    name: "{{ monitor_notify_packages }}"
    state: present
    update_cache: true
  when: monitor_notify_enabled
