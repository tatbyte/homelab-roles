---
# roles/monitor_notify/tasks/main.yml
# Task entrypoint for the `monitor_notify` role.

- name: "Assert | Validate monitor_notify variables"
  ansible.builtin.import_tasks: assert.yml

- name: "Install | Ensure monitor_notify packages are installed"
  ansible.builtin.import_tasks: install.yml

- name: "Config | Render and manage monitor_notify artifacts"
  ansible.builtin.import_tasks: config.yml

- name: "Validate | Assert monitor_notify final state"
  ansible.builtin.import_tasks: validate.yml
