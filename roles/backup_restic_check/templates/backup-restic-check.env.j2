{# roles/backup_restic_check/templates/backup-restic-check.env.j2 #}
{# Template for the managed Restic environment file in `backup_restic_check`. #}
# Managed by Ansible: backup_restic_check
# Source: roles/backup_restic_check/templates/backup-restic-check.env.j2
RESTIC_REPOSITORY={{ backup_restic_check_repository | string | to_json }}
RESTIC_PASSWORD={{ backup_restic_check_password | string | to_json }}
{% for item in backup_restic_check_extra_environment | dictsort %}
{{ item.0 }}={{ item.1 | string | to_json }}
{% endfor %}
