---
# roles/monitoring_notify/tasks/install.yml
# Install phase tasks for the `monitoring_notify` role.

- name: "Install | Ensure monitoring_notify packages are installed"
  ansible.builtin.apt:
    name: "{{ monitoring_notify_packages }}"
    state: present
    update_cache: true
  when: monitoring_notify_enabled
