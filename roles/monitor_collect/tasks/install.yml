---
# roles/monitor_collect/tasks/install.yml
# Install phase tasks for the `monitor_collect` role.

- name: "Install | Ensure monitor_collect packages are installed"
  ansible.builtin.apt:
    name: "{{ monitor_collect_packages }}"
    state: present
    update_cache: true
  when: monitor_collect_enabled
