---
# roles/monitoring_collect/tasks/install.yml
# Install phase tasks for the `monitoring_collect` role.

- name: "Install | Ensure monitoring_collect packages are installed"
  ansible.builtin.apt:
    name: "{{ monitoring_collect_packages }}"
    state: present
    update_cache: true
  when: monitoring_collect_enabled
