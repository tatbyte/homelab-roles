---
# roles/monitoring_collect/tasks/main.yml
# Task entrypoint for the `monitoring_collect` role.

- name: "Assert | Validate monitoring_collect variables"
  ansible.builtin.import_tasks: assert.yml

- name: "Install | Ensure monitoring_collect packages are installed"
  ansible.builtin.import_tasks: install.yml

- name: "Config | Render and manage monitoring_collect artifacts"
  ansible.builtin.import_tasks: config.yml

- name: "Validate | Assert monitoring_collect final state"
  ansible.builtin.import_tasks: validate.yml
