---
# roles/monitoring_status_now/tasks/main.yml
# Task entrypoint for the `monitoring_status_now` role.
# Imports the assert and validate phase files in order.

- name: Assert
  ansible.builtin.import_tasks: assert.yml
  tags: [assert, monitoring_status_now, monitoring_status_now_assert]

- name: Validate
  ansible.builtin.import_tasks: validate.yml
  tags: [validate, monitoring_status_now, monitoring_status_now_validate]
