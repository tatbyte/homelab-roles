---
# roles/monitoring/meta/main.yml
# Role metadata for `monitoring`.
# Leaves dependencies empty because aggregate monitoring-role ordering is
# defined in `roles/monitoring/tasks/main.yml`.

dependencies: []
