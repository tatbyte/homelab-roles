---
# roles/backup_restic_prune/tasks/main.yml
# Task entrypoint for the `backup_restic_prune` role.
# Imports the assert, install, config, and validate phase files in order.

- name: Assert
  ansible.builtin.import_tasks: assert.yml
  tags: [backup_restic_prune, assert, backup_restic_prune_assert]

- name: Install
  ansible.builtin.import_tasks: install.yml
  tags: [backup_restic_prune, install, backup_restic_prune_install]

- name: Config
  ansible.builtin.import_tasks: config.yml
  tags: [backup_restic_prune, config, backup_restic_prune_config]

- name: Validate
  ansible.builtin.import_tasks: validate.yml
  tags: [backup_restic_prune, validate, backup_restic_prune_validate]
